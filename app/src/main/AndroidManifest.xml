<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.syh4834.chabak">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature android:name="android.hardware.camera" />

    <application
        android:requestLegacyExternalStorage="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="차박2일"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".report.ReportActivity"></activity>
        <activity android:name=".home.BannerDetailActivity" />
        <activity android:name=".splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".auth.SignupSuccessActivity" />
        <activity android:name=".placeDetail.MapActivity" />
        <activity android:name=".review.ReviewUploadActivity" />
        <activity android:name=".review.ReviewTotalActivity" />

        <meta-data
            android:name="com.naver.maps.map.CLIENT_ID"
            android:value="1tsbt7auhh" />

        <activity android:name=".placeDetail.PlaceDetailActivity" />
        <activity android:name=".auth.SigninActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".InitialActivity" />
        <activity android:name=".auth.SignupActivity" />
    </application>

</manifest>